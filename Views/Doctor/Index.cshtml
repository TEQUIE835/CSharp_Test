@model IEnumerable<Doctor>
@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        function deleteConfirmation(id) {
            Swal.fire({
                title: '¿Are you sure?',
                text: "You won't be able to revert this!",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, delete',
                cancelButtonText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = '/Doctor/Delete/' + id;
                }
            });
        }

    </script>
    <script>
        async function editDoctor(id) {
            try {
                const response = await fetch(`/Doctor/GetById/${id}`);
                if (!response.ok) {
                    Swal.fire('Error', 'No se pudo obtener el libro.', 'error');
                    return;
                }

                const doctor = await response.json();
    Swal.fire({
        title: 'Edit doctor',
        html: `
            <div class="mt-3 d-flex flex-column">
                <label for="">Name</label>
                <input type="text" id="Name" class="form-control" placeholder="Name" value="${doctor.name}">
            </div>
            <div class="mt-3 d-flex flex-column">
                <label for="">Document</label>
                <input type="text" id="Document" class="form-control" placeholder="Document" value="${doctor.document}">
            </div>
            <div class="mt-3 d-flex flex-column">
                <label for="">Email</label>
                <input type="text" id="Email" class="form-control" placeholder="Email" value="${doctor.email}">
            </div>
            <div class="mt-3 d-flex flex-column">
                <label for="">Phone</label>
                <input type="text" id="Phone" class="form-control" placeholder="Phone" value="${doctor.phone}">
            </div>
            <div class="mt-3 d-flex flex-column">
                <Select class="form-select" name="Speciality" id="Speciality">
                   <option value="Pediatric">Pediatric</option>
                   <option value="Neumology">Neumology</option>
                   <option value="Neurology">Neurology</option>
                </Select>
            </div>
          
        `,
        showCancelButton: true,
        confirmButtonText: 'Save',
        cancelButtonText: 'Cancel',
        focusConfirm: false,
        preConfirm: () => {
            const Name = document.getElementById('Name').value.trim();
            const Document = document.getElementById('Document').value.trim();
            const Email = document.getElementById('Email').value.trim();
            const Phone = document.getElementById('Phone').value.trim();
            const Speciality = document.getElementById('Speciality').value.trim();

            
            if (!Name || !Document || !Email || !Phone || !Speciality) {
                Swal.showValidationMessage('Por favor completa todos los campos');
                return false;
            }

            return { Name, Document, Email, Phone, Speciality };
        }
    }).then(async (result) => {
        if (result.isConfirmed) {
            const updatedDoctor = {
                Id : id,
                Name: result.value.Name,
                Document: result.value.Document,
                Email: result.value.Email,
                Phone: result.value.Phone,
                Speciality: result.value.Speciality,
            };
            
             
                const res = await fetch(`/Doctor/Edit`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(updatedDoctor)
                });
                const data = await res.json();

                if (data.success) {
                    Swal.fire('Éxito', data.message, 'success').then(() => location.reload());
                } else {
                    Swal.fire('Error', data.message, 'error');
                }
            

            
        }
        })}catch (error) {
            console.error(error);
            Swal.fire('Error', 'Ocurrió un error al editar el libro.', 'error');
        }}
        
    
</script>
    <script>
        @if (TempData["DeleteSuccess"] != null)
        {
            <text>
                Swal.fire({
                    icon: 'success',
                    title: '¡Success!',
                    text: '@TempData["DeleteSuccess"]'
                });
            </text>
        } else if (TempData["DeleteError"] != null)
        {
            <text>
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: '@TempData["DeleteError"]'
                });
            </text>
        } else if (TempData["CreateSuccess"] != null)
        {
            <text>
                Swal.fire({
                    icon : 'success',
                    title : 'Success!',
                    text : '@TempData["CreateSuccess"]'
                })
            </text>
        }else if (TempData["CreateError"] != null)
        {
            <text>
                Swal.fire({
                    icon : 'error',
                    title : 'Error',
                    text : '@TempData["CreateError"]'
                })
            </text>
        }
    </script>
}


<h1>Doctor list</h1>

<div class="row">
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <form asp-action="AddDoctor">
                    <div class="mt-3 d-flex flex-column">
                        <label for="">Name</label>
                        <input type="text" class="form-control" name="Name" required>
                    </div>
                    <div class="mt-3 d-flex flex-column">
                        <label for="">Document</label>
                        <input type="text" class="form-control" name="Document" required>
                    </div>
                    <div class="mt-3 d-flex flex-column">
                        <label for="">Email</label>
                        <input type="text" class="form-control" name="Email" required>
                    </div>
                    <div class="mt-3 d-flex flex-column">
                        <label for="">Phone</label>
                        <input type="text" class="form-control" name="Phone" required>
                    </div>
                    <div class="mt-3 d-flex flex-column">
                            <Select class="form-select" name="Speciality">
                                <option value="Pediatric">Pediatric</option>
                                <option value="Neumology">Neumology</option>
                                <option value="Neurology">Neurology</option>
                            </Select>
                    </div>
                    <hr>
                    <button type="submit" class="btn btn-primary w-100">Save information</button>
                </form>
            </div>
        </div>

    </div>
    <div class="col-md-9">
        <form asp-action="Filter">
            <Select class="form-select" name="filter">
                <option value="Pediatric">Pediatric</option>
                <option value="Neumology">Neumology</option>
                <option value="Neurology">Neurology</option>
            </Select>
            <hr>
            <button type="submit" class="btn btn-primary w-100"> filter</button>
        </form>
        <hr>
        <table class="table">
            <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Document</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Speciality</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Document</td>
                    <td>@item.Email</td>
                    <td>@item.Phone</td>
                    <td>@item.Speciality</td>
                    <td>
                        <a class="btn btn-primary" onclick="editDoctor(@item.Id)">Edit</a>
                    </td>
                    <td>
                        <button class="btn btn-danger" onclick="deleteConfirmation(@item.Id)">Delete</button>
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Doctor" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>